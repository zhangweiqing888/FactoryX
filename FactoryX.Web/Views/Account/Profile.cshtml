@model FactoryX.Application.DTOs.Responses.UserManagementResponses.GetUserProfileResponse
@{
    ViewData["Title"] = "我的资料";
}
<partial name="_Alert" />
<h2 class="fw-bold mb-4">个人资料</h2>
<form asp-action="Profile" method="post" class="glass-card p-4 shadow-sm" autocomplete="off">
    <input type="hidden" asp-for="Id" />
    <div class="mb-3">
        <label asp-for="UserName" class="form-label">用户名</label>
        <input asp-for="UserName" class="form-control" />
        <span asp-validation-for="UserName" class="text-danger small"></span>
    </div>
    <div class="mb-3">
        <label asp-for="Email" class="form-label">邮箱</label>
        <input asp-for="Email" class="form-control" />
        <span asp-validation-for="Email" class="text-danger small"></span>
    </div>
    <div class="mb-3">
        <label asp-for="FullName" class="form-label">姓名</label>
        <input asp-for="FullName" class="form-control" />
        <span asp-validation-for="FullName" class="text-danger small"></span>
    </div>
    <button type="submit" class="btn btn-primary rounded-pill px-4">保存</button>
    <a asp-action="ChangePassword" class="btn btn-outline-secondary ms-2">修改密码</a>
</form>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
<style>
    .glass-card {
        background: rgba(255, 255, 255, 0.7);
        border-radius: 1rem;
        backdrop-filter: blur(6px);
        max-width: 500px;
        margin: auto;
    }

    [data-bs-theme="dark"] .glass-card {
        background: rgba(30, 30, 40, 0.7);
    }
</style>